import { Formula } from '../../src/contracts/formula';

const FORMULA: Formula = {
  name: 'MySQL',
  shell: '/bin/bash',
  logo: 'some-logo',
  image: 'some-container-image',
  env: {},
  data: {}
};

export { FORMULA };
